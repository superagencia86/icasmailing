.title
  - if @source_list
    %span=  "Guardar lista de envío '#{@source_list.name}' con otro nombre"
  - else
    %span=  "Crear lista de envío #{@subscriber_list.auto_update? ? 'inteligente' : 'normal'}"

- if @subscriber_list.auto_update?
  %h2 Una lista inteligente actualiza automáticamente cuando se añaden nuevos contactos con los mismos criterios que los de la lista.

- form_for @subscriber_list do |f|
  .remote
    .section
    = render :partial => "form", :locals => {:f => f}

  .buttonbar
    = f.submit((@subscriber_list.new_record? ? 'Crear lista' : 'Guardar lista'), :class => 'btn')
    = link_to('Cancelar', (@subscriber_list.new_record? ? subscriber_lists_path : subscriber_list_path(@subscriber_list)))
