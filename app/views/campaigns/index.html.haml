.title_tools
  = link_to("Crear campaña", new_campaign_path, :title => "Crear campaña")

.title
  %span= "Campañas"

#paginate= render :partial => "common/paginate"
.list#campaigns
  - unless @campaigns.empty?
    = render :partial => "campaign", :collection => @campaigns
  - else
    = render :partial => "common/empty"

