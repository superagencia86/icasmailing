= f.hidden_field :user_id, :value => current_user.id
.remote
  .section
    %table(width="500" cellpadding="0" cellspacing="0")
      %tr
        %td(valign="top")
          .label.top.req= "Nombre"
          = f.text_field :name, :style => "width:324px"
        %td= spacer
        %td
          .label.top.req= "Empresa"
          #company
            = render :partial => 'companies/company_select', :locals => {:f => f.object_name, :selected => @proposal.company_id}

      %tr
        %td{ :valign => :top }
          .label.top Descripción
          = f.text_area :description, :style =>"width:324px", :rows => 4
        %td= spacer
        %td{ :valign => :top }
          .label.top= "Estado"
          = f.collection_select :state, PROPOSAL_STATE, :idx, :name, :selected => (@proposal.state.to_sym if @proposal.state.present?), :include_blank => true
