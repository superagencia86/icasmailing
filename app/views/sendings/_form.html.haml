- form_for @sending, :url => [@campaign, @sending] do |f|
  = f.error_messages
  .remote
    .section
      %table(width="500" cellpadding="0" cellspacing="0")
        %tr
          %td(valign="top")
            .label.top.req= "Lista a la que se envía:"
            = f.collection_select :subscriber_list_id, current_space.subscriber_lists, :id, :info, {:selected => params[:clone].to_i}
        %tr
          %td(valign="top")
            .label.top.req= "Descripción del envío:"
            = f.text_area :description

  .buttonbar
    = f.submit 'Realizar envío', :class => 'btn'