%h1= "Contactos del espacio #{@space.name}"
= will_paginate @contacts
%br/
%br/
%br/
%table
  %thead
    %tr
      %td
      %td Id
      %td Email
      %td Nombre
      %td Espacio
      %td Incluído
      %td Enlaces
  %tbody
    - @contacts.each do |contact|
      %tr
        %td= contact.confirmed? ? 'c' : 'x'
        %td= contact.id
        %td= contact.email
        %td= contact.name
        %td= contact.space.name
        %td= time_ago_in_words contact.created_at
        %td
          = "<b>Aceptar suscripción:</b> http://#{APP.host}#{ConfirmationsController::ACCEPT_URL}/#{contact.confirmation_code}<br/>"
          = "<b>Cancelar suscripción:</b> http://#{APP.host}#{ConfirmationsController::REJECT_URL}/#{contact.confirmation_code}"


