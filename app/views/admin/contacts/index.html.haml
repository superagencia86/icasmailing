%h1 Gestión de contactos (administrador)
= will_paginate(:prev_label => '« Anterior', :next_label => 'Siguiente »')
%br/
%br/
%br/
%table
  %thead
    %tr
      %td
      %td Id
      %td Email
      %td Nombre
      %td Espacio
      %td Incluído
      %td Enlaces
  %tbody
    - @contacts.each do |contact|
      %tr
        %td= contact.confirmed? ? 'c' : 'x'
        %td= contact.id
        %td= contact.email
        %td= contact.name
        %td= contact.space.name
        %td= time_ago_in_words contact.created_at
        %td
          = "<b>Aceptar suscripción:</b> #{confirm_url(contact)}<br/>"
          = "<b>Cancelar suscripción:</b> #{unconfirm_url(contact)}<br/>"


