.title
  %span= "Editar usuario"

- form_for [:admin, @space, @user] do |f|
  = f.error_messages 
  = render "form", :f => f

  .buttonbar
    = f.submit "Editar usuario", :class => "btn"
    = link_to 'Cancelar', admin_users_path

